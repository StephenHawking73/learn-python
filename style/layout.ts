import { StyleSheet } from "react-native";

export const layout = StyleSheet.create({
  container: {
    flex: 1,
    padding: 24,
  },

  center: {
    justifyContent: "center",
    alignItems: "center",
  },
});
